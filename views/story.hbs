{{#section 'pageTitle'}}
<title>Story!</title>
{{/section}}

<article class="container">
	<div class="rcw text-center">
		<h1>{{story.title}}
			<br>
			<small>{{story.subTitle}}</small>
		</h1>
		{{story.dateCreated}}
	</div>
	<p>
	{{story.content}}
	</p>
</article>

