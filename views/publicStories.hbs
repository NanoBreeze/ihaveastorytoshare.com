<div class="container">
    <h2>All stories</h2>
    <div class="row">
        {{#each storyPreviews}}
            <div class="col-sm-4 col-md-3">
                {{> storyPreview this}}
            </div>
        {{/each}}
    </div>
</div>